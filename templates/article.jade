mixin article(page)
  article.post
    header
      h2: a(href=page.url)= page.title
      h5= moment(page.date).format('LLL')
    section.content!= page.html

extends layout

block prepend title
  | #{ page.title } - 

block content
  mixin article(page)
